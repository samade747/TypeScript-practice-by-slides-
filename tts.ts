// type TrafficLight = "red" | "yellow" | "green";

// let currentLight: TrafficLight;

// currentLight = "red"; // valid, as "red" is in the list of allowed values
// currentLight = "blue"; // error, as "blue" is not in the list of allowed values


let TrafficLight : "red" | "yellow" | "green";

TrafficLight = "red"
TrafficLight = "yellow"
TrafficLight = "green"

console.log(TrafficLight)



